server:
  port: ${CUSTOM.SERVER.PORT:7077}

spring:
  application:
    name: product-service
  datasource:
    url: jdbc:oracle:thin:@//localhost:1521/XEPDB1
    username: igor
    password: Igor_1234
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    hibernate:
      ddl-auto: none
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.OracleDialect
