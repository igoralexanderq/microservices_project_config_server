eureka:
  client:
    fetchRegistry: true
    registerWithEureka: true
    serviceUrl:
      defaultZone: ${EUREKA.SERVER.HOSTNAME:http://discovery-server.drakium.com:8761/eureka/}
  instance:
    leaseRenewalIntervalInSeconds: 2
    leaseExpirationDurationInSeconds: 2

spring:
  cloud:
    config:
      uri: http://config-server.drakium.com:9000
      #username: alexander
      #password: vladivostok